# Classic compatibility gaps

This register tracks the work needed to run compiled Classic scenarios as
Remake campaigns. It is intentionally separate from opcode coverage: an opcode
may already have interpreter semantics while still waiting for a native Remake
bridge, and an unsupported opcode may map cleanly to an existing Remake system.

## Classification

Priority describes the effect on a scenario rather than the size of the work:

- **Required**: missing behavior can block progression, change authored state or
  outcomes, or prevent a scenario from being installed, played, or saved.
- **Recommended**: the current fallback preserves progression and the authored
  result, but presentation, pacing, or interaction differs from Classic.

Implementation status describes the Remake side of the gap:

- **Implemented**: the completion criterion has focused integration coverage.
- **Native, unwired**: Remake has a suitable system, but the Classic adapter does
  not yet call it.
- **Partial**: some variants work, while others still stop or use a fallback.
- **Absent**: Remake has no corresponding system yet.
- **Resource gap**: the runtime path exists, but converted data or Remake
  resources do not preserve what it needs.
- **Unknown**: the closest native behavior or the Classic contract still needs
  source and fixture research.

Evidence is recorded as **fixture-proven**, **source-backed**, **inferred**,
**malformed**, or **unknown**. The
[multi-scenario regression corpus](CLASSIC_REGRESSION_CORPUS.md) keeps all five
categories distinct for audited actions. A row moves to completed only after
its completion criterion is covered by a focused fixture or playtest.

## Required compatibility work

| Capability | Remake status | Evidence | Current boundary | Completion criterion |
| --- | --- | --- | --- | --- |
| Campaign discovery and launch | Implemented | Fixture-proven | Installed compiled bundles are discovered by `campaign.json`. The normal campaign panel shows manifest title, version, compatibility profile, readiness state, summary, and the first actionable blocker; a ready package starts its compatibility session and native start map through the standard party flow. | A compiled campaign can be discovered, selected, and started through the normal campaign UI. |
| Party admission and scenario rules | Implemented | Source-backed, fixture-proven | Campaign selection preserves the scenario header's recommended and maximum total-party levels separately from `Data RI`'s maximum party size, per-character level gate, banned races, banned castes, and authored description. The normal party UI enforces those admission rules and exposes an actionable reason on rejected characters. Exact rule-name matching and saved Classic IDs bridge native character resources to campaign identities; producer-resolved changed records receive the active profile, while unresolved tables remain blocked rather than falling back to stock mechanics. Producer selection evidence distinguishes shared tables, supported scenario-local tables, and unresolved imports; older or ambiguous bundles remain conservative. Active changed records retain their stable Classic identities and replace the native race/caste movement contributions without discarding equipment modifiers. Their race attack base, caste starting bonus, level thresholds, and race maximum also replace native action progression while retaining Classic half-attacks as Remake's alternating-round fractional actions. On later level-ups, caste `toHit`, `dodge`, `missile`, and `hand2Hand` gains replace the corresponding native caste growth and remain stored on the character after save/load or scenario exit; their creation bases are deliberately not retrofitted onto established characters. To-hit and hand-to-hand growth retain their source increments, dodge uses Remake's coarser five-percentage-point evasion units, and missile skill rolls from one through the source maximum before conversion to Remake's five-percentage-point accuracy units and its equivalent 0–100 cap. Caste stamina growth likewise replaces native race/class HP growth with Classic's inclusive random roll plus its capped Vitality-over-16 bonus; current and maximum HP rise together, preserving the existing injury deficit through ordinary saves. Caste spellcasting rows now preserve Classic's level-up school precedence and start/maximum levels. Applying an active caster row stores that school on the character so it can disambiguate already learned Classic spell identities immediately. Each later level replaces native SP growth with zero before the authored start level or with Classic's school-specific attribute roll plus character level once eligible; current and maximum SP rise together while preserving spent points through ordinary saves. The same source rows now drive a derived spell-selection budget: their combined start levels determine effective caster level, their combined maximum levels cap the visible school catalog, current Intellect or Wisdom supplies the source bonus, and known spells consume the exact `1, 3, 6, 10, 15, 21, 28` cost ladder. Remake's existing ability-management panel uses that derived budget without writing it to generic `selection_pts`, offers only the active Classic school, and prunes over-budget known spells in Classic traversal order when opened; native characters retain their existing selection-point behavior. Dedicated creation adapters now replace the native level-one attributes, demographics, initial SP pool, and mundane combat bases before advanced-level progression. The attribute pass enforces `canCaste`, rolls and clamps all six source attributes in Classic order, applies gender and minimum-age-group changes, writes the five shared attributes through native base stats, and saves Luck, gender, age, and age group as compatibility state. The combat pass uses Classic's stamina roll plus capped Vitality bonus, Strength-derived to-hit and capped damage, Dexterity-derived armor class and dodge, race-plus-caste missile skill with the caste permission gate, and the caste hand-to-hand die; percentage fields use Remake's existing five-percentage-point scale and its coarser evasion rounding. The spell pass uses the distinct Sorcerer, Priest, or Enchanter formula, starts current SP full, and preserves the source's unusual independent-row overwrite behavior. Established characters are never refilled or rebuilt. From a selected ready Classic campaign, the normal New Character panel exposes active stock and producer-identified changed race/caste choices, applies the ordered creation adapters before ordinary advanced-level progression and spell selection, and assigns the result to that campaign. All forty race starting-condition slots and caste level-one conditions now apply through reviewed mappings without partial fallback. Caste level thresholds for the translated progression conditions grant or strengthen their saved permanent traits at the exact source level. When both active rule rows are available, compatibility-owned character state preserves Classic's creation-time magic-resistance formula and its one-point level-up roll based on Intellect, Wisdom, and Vitality through normal saves. Changed profiles now enforce both race and caste item-category masks through Remake's existing player equipment gate while preserving native item and slot restrictions. A one-time creation-resource adapter replaces native gifts with the active caste's ordered starting items and gold, resolves every item before mutation, follows native carry and equipment paths, and preserves its result through save/load. The fourteen special-ability slots initialize from the race and caste's exact source rows, including Strength and Dexterity modifiers, percentage caps, uncapped Pick Pocket and Turn Undead, and the racial Turn Undead exception. Compatible level-ups roll each authored caste maximum and then enforce Classic's deterministic current-level Turn Undead floor; the array survives ordinary saves. Exact age days and the current age group now survive normal character saves. Every crossed Remake midnight invokes the source aging routine once, while attacks and spells can submit their larger day changes through the same adapter; each call moves at most one band and applies or reverses that row's attributes, Strength-owned combat bonuses, magic resistance, movement floor, and first seven saving throws. Combat rewards now preserve that separate maximum-age consequence: after the ordinary eligible-party split, a character at or beyond the race's `maxAge` receives the source's truncated two-thirds share. Scripted awards and Turn Undead bonuses remain unaffected, and the unused `doesNotDie` race field has not been assigned invented behavior. Ordinary inventory activation now shares the same source category-mask gate as equipment, while encounter item responses retain Classic's explicit bypass. Producer-resolved scenario-local records are admitted; unresolved older table selection remains blocked conservatively. | Covered. |
| Known scenario custom rules | Partial | Corpus-audited, fixture-proven | The [checked custom-rule manifest](KNOWN_CUSTOM_RULE_AUDIT.md) inventories 39 available scenarios, deduplicates spell/race/caste payloads by raw hash, retains campaign-scoped source locations, and joins 221 active spell definitions to 1,114 represented consumers. Empty templates, representable customs, unsupported active specials, no-op race/caste tables, and active changed tables have focused readiness fixtures. Inactive unsupported definitions warn without blocking; malformed legacy data remains separate. | Bundle version 1 still does not represent temple offerings, learned-spell lists, or scroll catalogs. The 123 active nonzero-special definitions require bounded mechanics work before their 15 scenarios can be called ready. |
| Scenario race/caste identity consumers | Implemented | Source-backed, fixture-proven | Active changed records retain their stable Classic IDs, race descriptors, and caste class. Opcode `50` uses them for race, gender, caste, race-class, and caste-class picked-character sets. Opcode `86` uses the same resolver for saveable party branches, including selected-only identity checks, strict party/selected total-level thresholds, and native boat/camp state. Exact restrictions retained on generated Classic item records are evaluated against the active custom identity before Remake's generated stock-name permissions, while native-only item restrictions remain unchanged. | Covered. |
| Scenario race foe bonuses | Implemented | Source-backed, fixture-proven | Changed race profiles preserve all eight `plusMinusToHit` entries separately from Classic's fourteen skill slots. Each matching generated or native bestiary tag adds five percentage points of melee accuracy and one damage per source point; multi-type monsters stack their entries. The shared native combat seam covers ordinary and custom melee weapons, including critical scaling, while characters without an active Classic profile remain unchanged. | Covered. |
| Scenario caste victory progression | Implemented | Source-backed, fixture-proven | Changed caste profiles preserve all thirty `victory` requirements. Normal Remake reward delivery keeps its UI and overflow loop, but each completed level loads the next active Classic requirement, with levels above thirty capped to the final source row. Existing character progress is not rewritten when the active scenario table changes, and the profile survives ordinary save/load. | Covered. |
| Save and load integration | Implemented | Fixture-proven | Normal profile saves include a versioned Classic envelope containing campaign identity, persistent runtime and adapter state, and safe suspended continuations. Encounter attempts and results, GOSUB frames, battle-return state, and deferred action-point mutations are plain-data snapshots; load restores the native map and HUD before replaying the pending command. Live native combat and side-effecting rogue-response intervals are rejected with an actionable message because replay would duplicate state that Remake does not serialize. Version-one envelopes migrate as idle continuations, future schemas are rejected before the existing save changes, and the compiled bundle remains immutable. | A normal save and load round trip preserves Classic state and resumes supported suspended action lists at their authored slot without duplicating effects. |
| Classic map bridge | Partial | Fixture-proven | Typed map commands resolve the conventional `map_<level>` / `mapd_<level>` resources, use `GameGlobal.change_map()` for campaign starts and land/dungeon transitions, move within a loaded map, redraw view changes, project darkness, trigger percentages, and random rectangles into native map data, and copy changed Classic tiles from an immutable native reference palette. The package installer deterministically materializes missing outdoor native maps from complete normalized tile arrays. It also materializes signed dungeon fields as a campaign-local native tileset composed from Realmz's PICT 302 overhead sprites, retaining each source field as tile metadata and matching wall, door, note-cell, and Action Point movement exceptions. Directional dungeon secrets admit only their authored cardinal entry bits; first entry reveals the generated native tile and persists the changed field. A decoded 640 x 320 custom atlas and compiled behavior table become a campaign-local 200-tile native landlook with movement, sight, water, shore, timing, path, clear-land, combat-build, and sound identities retained. Generated numeric tile sounds use the existing Classic audio adapter, with native filename-based map sounds taking priority and bundle runtime media preferred over legacy Remake mappings. Decoded 32 x 32 special-land media becomes a campaign-local overlay over the source landlook, retaining the raw negative field, normalized `cicn` identity, and source `Data Solids` movement rule. Classic `needBoat=1` cells materialize as source-backed underlying terrain plus native boat placements; campaign start seeds Remake's existing boarding, sailing, docking, and save-state lifecycle without replacing restored boat state. Stable trigger IDs, chances, and random rectangles use Remake's existing map format, and the normal resource loader retains generated map metadata for campaign-start seeding. Undecoded custom atlases, special land tiles without suitable decoded media, incomplete tile arrays, and unsupported render modes stop installation instead of receiving a lossy fallback. The host reapplies persistent darkness, landlook, random-rectangle, moved/replaced Action Point, trigger-percent, and tile mutations after native resources load. The checked City of Bywater playtest verifies a rendered land-to-dungeon-to-land transition through existing native resources. Classic board/disembark confirmation timing, scripted boat-state opcodes, and a producer-derived generated-map visual smoke remain open. | A producer-generated land-to-dungeon-to-land fixture installs without hand-authored map files and preserves its rendered terrain, overlays, triggers, boats, and persistent mutations. |
| Battles and battle context | Partial | Source-backed | Basic, branching, and selective battle requests now resolve native `Battle_<id>` resources, preserve pre-battle presentation and loot modes, wait for normal Remake combat cleanup, and resume the Classic action list. Missing native battle resources are materialized from compiler-produced 13x13 `Data BD` grids with resolved bestiary participants, Classic identity metadata, signed side flips, distance, round macro, and request-specific priest-turning state. The installer also materializes supported compiler-produced monster records through the normal campaign Bestiary lifecycle, preserving the complete source record, both Classic identities, core combat statistics, size, type tags, physical attacks, and a deterministic reward estimate. Shared icon art, averaged random statistics, and omitted Classic difficulty/reward rolls remain explicit fidelity fallbacks. All six carried-item slots now resolve through campaign-first Classic identity into the normal native item book, and a concrete positive `weapon` ID equips that inventory object for both allies and combatants. When Realmz keeps the active weapon separate from those six loot slots, the adapter adds a non-lootable native equipment instance so combat and victory rewards retain the source distinction. Packed monster spell slots resolve through the established Classic mapping into available native spell resources, preserve repeated-slot weighting, and reject a same-name resource whose explicit Classic IDs describe different mechanics. Unresolved item or spell IDs, unsupported item fields, non-equippable weapons, and Classic's negative random-weapon table selectors remain launch blockers. General magic resistance remains compatibility-owned instead of being approximated as native magic evasion or damage resistance; mapped and compiler-produced resistible spells make the Classic all-or-nothing roll against preserved monster metadata plus worn-item modifiers before native spell effects run. The same boundary preserves all six monster saving throws and immunity flags, including distinct Charm and Mental values, for Classic spell and encounter checks without expanding Remake's native stat model. Unarmed elemental special attacks `11` through `15` materialize as native Fire, Ice, Electric, Chemical, and Mental damage while retaining their Classic code; native resistance replaces Classic's separate per-hit save and protection checks as an explicit fidelity fallback. Monster requirements for blunt, sharp, exact-ID, and minimum-magic-plus weapons pass through the normal Bestiary loader and gate native melee accuracy; Remake uses its ordinary miss feedback instead of Classic's requirement-specific warnings. Template fields that Realmz overwrites during battle setup no longer block materialization. Source run and surrender values from 0 through 100 remain inert, matching Realmz's stamina-over-itself morale check. At a value above 100, surrender takes source-order priority and removes the living monster through native non-death cleanup; `101` retains the panic presentation. Otherwise an active run value applies Remake's permanent fleeing AI and Classic edge exit. Unarmed condition specials `1` through `7` and `16` now reuse native temporary traits while preserving Classic save families, rolled durations, the party's 30-round cap, and permanent-condition exclusions. Unarmed special `8` transfers three spell points per attacker hit die, special `9` removes twenty experience per attacker maximum stamina from party targets, and special `10` applies battle-scoped native Charm with party resistance, Undead immunity, and monster retargeting. Unarmed specials `18` and `19` reuse the translated permanent Blind and Petrified traits after the Classic special save. Classic monsters now use 1-to-100 missile-first and cast decision rolls once per round, suppress casting after successful native melee or direct damaging spell hits, choose power from 1 through 7, lower it until affordable, and retain their authored spell-attack count. When movement ends without an attack, a nonzero cast percentage receives Realmz's one unconditional retry without another percentage roll; failed spell passes and melee attempts prevent the retry from looping. Missile decisions preserve the resolved carried-item-slot-two identity and use only that charged combat-spell item; a missing, depleted, or non-spell slot-two item remains a launch blocker without changing native Remake AI. A deterministic custom-monster fixture carries caster, save and immunity, missile and melee, inert and active morale, summon, loot, faction, round-macro, and death-macro data through bestiary and native battle materialization. Successful hostile scaled effects and zero-damage conditions now preserve pre-effect faction hostility and set the same round-scoped attacked state as direct damage; round-start poison and disease remain excluded because Realmz clears the flag again after upkeep. Weapon-coupled specials and aging special `17` remain blockers. Authored cowardice outcomes show Classic's core warnings and mapped sound, then remove 2,000 experience per character level once; Remake's inverse `exp_tnl` representation is handled explicitly. Successful land movement supplies the original entry delta so cowardice also returns the party to its previous tile; dungeon outcomes preserve Classic's no-retreat behavior, and triggers without movement context report the skipped retreat. Opcode `48` supplies only the picked living characters, prevents their defeat from becoming a whole-party game over, and preserves its survivor-dependent fixed treasure. Opcode `127` queries the native live roster by the preserved Classic monster name byte and stops its combat macro when the required monster is absent. Opcodes `121` and `125` remove eligible live combatants through the native roster, using Data MD record IDs and monster name bytes respectively, preserve Classic's undead-tier and faction rules, and register hostile removals with battle rewards. Opcode `123` filters its compiled monster record IDs to the macro actor's faction, applies Remake's permanent fleeing AI, and removes routed combatants through native cleanup when they reach the battlefield's outer inset. Classic's ally sentinel prevents the exit. Other routed hostiles remain reward-eligible; routed allies leave without entering rewards, and neither path fires a death macro. Opcode `124` resolves compiled monsters into native combatants, places them near the macro actor, adds them to the live roster and initiative, and preserves both Classic identity values plus count, capacity, sound, and faction rules. The native bridge invokes opcode `126` schedules at one-based round boundaries and supplies live battle-macro context. Deaths capture their macro, actor position, faction, and Classic identities before removal; the bridge drains that FIFO asynchronously before checking battle victory, including entries added by another death macro. Opcode `100` ends a live native battle as a forced victory, uses the normal cleanup flow with experience-only rewards, terminates its combat macro, and carries Classic's slot-8 sentinel back to the suspended outer action point so no later action runs. Opcodes `82` and `83` gate a once-per-character native Turn Undead action. Compiler metadata preserves the original target flags, hit dice, magic resistance, and summon sentinel; successful destruction uses normal death and reward handling, while turned targets join the player faction. | Authored battles respect priest-turning availability, execute combat action points and macros at native lifecycle boundaries, and resume suspended outer action lists after forced combat exits. |
| Monster initial conditions | Partial | Source-backed | A negative value in Classic condition slot `10` is permanent regeneration; generated bestiary entries and direct battle metadata retain its absolute healing amount, and the normal creature round lifecycle applies it only to a living, injured combatant. Negative values in slots `16` through `20` are permanent spell screens; compatibility-owned combat resolution stops spells at or below the strongest screen level before ordinary magic resistance. City of Bywater monsters 62 and 71 supply the checked regeneration and level-one screen cases. Positive condition counters and every other nonzero starting condition remain launch blockers. | Every starting condition used by a supported compiler-produced monster has a tested native or compatibility-owned lifecycle. |
| Shops and restricted shops | Partial | Fixture-proven | Opcodes `6` and `73` load compiled stock, prices, availability, and the source-authored two-range transfer rule through native shop state. A focused UI smoke covers restricted purchases, pooled-first payment, cancellation continuation, and persistent depleted stock. Compiled scenario-local stock now materializes into the campaign's ordinary native item book when its behavior is supported; effect-bearing records remain explicit readiness blockers. | Every compiled shop item resolves to a native resource, including scenario-local stock. |
| Temple and banking services | Implemented | Source-backed | Opcodes `32` and `49` expose native temple and bank controls, including Classic temple prices, pooled payment, automatic bank-to-temple pool transfer, and the exact built-in banking warning. The services smoke proves that later actions wait for warning dismissal. | Covered. |
| Classic item identity | Partial | Fixture-proven | Shared mappings and optional `classicItemId`/`classicItemIds` metadata resolve native inventory checks, mutations, treasure, shops, and encounter responses without display-name guessing. The package installer now deterministically materializes compiler-produced scenario item rows into the campaign's normal `Items/stuff_book.json`, preserving stable IDs, source text when present, the complete compiled record, and safe carry, charge, equipment, and economic fields. Existing native definitions take priority, while missing source text receives an honest `Classic Item <id>` display fallback. Referenced records with unsupported effects or restrictions block launch instead of being approximated. Supported source categories translate to Remake's concrete weapon, quiver, belt, necklace, headwear, shield, glove, cloak, body armor, boot, scroll-case, ring, and ion-stone permission types. Scenario-local melee weapons with matching small- and large-target dice also carry physical, fire, ice, and electric damage through the ordinary item and equipment lifecycle. Bonuses against undead, demonic, and evil creatures use native bestiary tags and retain their source random ranges; blunt and sharp classifications are retained for the corresponding monster-weapon requirement. Standard specific-race, specific-caste, race-descriptor, and caste-class restrictions become native equip permissions. A positive melee magic-plus supplies its source-equivalent hit chance and physical-damage bonus through native stats. Positive Classic armor on equippable items maps directly to native melee and ranged evasion while retaining its source rating. Signed Classic strength, spell-point, and movement modifiers on equippable items map directly to native stats. Remake's coarser native evasion scale, its replacement of Classic's separate elemental save and protection checks, and its application of encumbrance to the final movement stat are explicit fidelity fallbacks. Categoryless equipment, categories without a native permission type, size-dependent damage, negative magic-plus or target bonuses, invalid weapon classifications, non-equipment restrictions, negative armor, armor on non-equippable items, and equipment modifiers on non-equippable items remain launch blockers. Active custom race/caste profiles use the retained Classic record and compatibility identity resolver, so generated stock-name permissions do not override exact source restrictions. Exact Classic item art, sound, curses, and scripted special fields are still open. | Every carried, awarded, required, and stocked item has an exported identity that resolves to a bundled or shared Remake resource. |
| Equipment and item-state actions | Partial | Fixture-proven | Opcodes `21`, `22`, `36`, and `38` use native inventories and preserve carried, worn, charge, replacement, stored-equipment, and interim-loot behavior. A focused native UI smoke preserves an active equipment capture through the Classic session envelope, rebuilds its items through the campaign resource loader, restores worn state and charges, and presents interim items through Remake's loot panel. Scenario-local melee weapons, body armor, and shields with supported category fields now equip through the normal player-character path; other mapped equipment uses the same native permission types but does not yet have focused integration coverage. Positive melee magic-plus, armor, signed strength, signed spell points, signed movement, and signed Classic magic resistance apply once through the ordinary equipment lifecycle. Worn magic-resistance values stack additively with the character or monster base percentage and survive the normal item serializer; older shared-item saves are migrated away from the former native-magic-evasion approximation. Standard item-specific Classic race and caste restrictions remain native permissions. Active scenario race and caste profiles additionally require the generated item's exact first Classic category in both source item-type masks through the same player-character equipment path; native-only items are unaffected, and the masks and category survive save/load. Spell-point equipment changes current and maximum SP together, preserving Classic's current-pool behavior. Remake's weighted movement capacity now consumes calculated `MaxMovement`, so native equipment and traits affect usable movement rather than only the displayed stat. Luck remains blocked because Remake has no normal Luck stat or implemented `luck_bonus` trait. Curses and `special1` through `special5` require the scripted-item boundary tracked by ISY-415. | Every compiled item resolves to a native resource, including scenario-local equipment. |
| Encounter-result payments | Implemented | Source-backed, fixture-proven, native smoke | Take Gold (`33`) atomically spends pooled and carried gold or gems through native wealth state and preserves its result branches. Insufficient funds leave all balances untouched, play Classic warning sound `6000`, and present the exact built-in warning `50` before the authored failure continuation. The modal receives keyboard focus and resumes the same interpreter continuation only after acknowledgement. | Covered. |
| Character conditions | Implemented | Source-backed, fixture-proven, native smoke | Give Condition (`43`) preserves party, picked, and living targets plus signed durations, sound, continuation, and Classic's party-wide positive-value clear. The maintained [condition compatibility matrix](CONDITION_COMPATIBILITY.md) assigns all forty source indexes to native or compatibility-owned mechanics. Positive values decay once per combat round or crossed field hour, negative values remain permanent, and saves snapshot live traits so an expired condition cannot reappear after reload. Disease now uses the same signed Classic reducer for opcode, spell, attack, field-time, and save paths. Classic-only temporary curse, slow, and elemental-protection traits keep their native combat effects without inheriting Remake's raw-second exploration decay. Fleeing uses retreat AI and the existing Classic edge-exit lifecycle without Remake's unrelated accuracy penalty. Helpless actors lose control and spellcasting; physical attacks bypass evasion and deal their current health. Positive Animated automates one turn, blocks spellcasting and spell-point recovery, grants no experience, and clears when that turn ends; negative Animated remains permanent. Turned to Stone forces the native dead state, blocks healing, and does not revive the character when a positive counter expires. Race creation, caste grants, actions, spells, monster attacks, combat upkeep, field time, and saves converge on the same traits and signed state. Readiness blocks any referenced out-of-range condition before launch. Native campaign UI for authoring scenario-local races and castes remains separate. | Covered. |
| Timed encounters | Implemented | Source-backed, fixture-proven | Imported land movement converts each source `timeclick()` to five in-game minutes outdoors or one minute indoors; successful dungeon steps normalize to one indoor click, including older generated map installs, and enabled Search checks pay their separate four-click cost. Native campaigns retain their existing clock scale. Remake's clock then submits every crossed scenario day to a source-ordered Classic scheduler. Effective records apply opcode `54` overrides and preserve `timeclick()`'s write and cursor behavior: chance, item, and quest failures persist the next day and stop the current scan, location mismatches continue without writing the candidate increment, and matches persist before dispatching their `Data ED3` action point through the normal host. Repeating and zero-increment one-shot schedules retain Classic behavior. Combat defers dispatch without losing the crossed day; completed-day suppression, pending scan cursors, and next schedules survive the normal save envelope. A macro resumes the same scan afterward so it can mutate a later record before evaluation. The installed-route City of Bywater check crosses day 3, dispatches XAP 163, and verifies that map 0 replaces its original landlook with source-numbered `landlook-10` generated from Realmz PICT 310. | Covered. |
| Modal map input and presentation | Partial | Native playtest | Get Click and Redraw Screen use the native HUD and map redraw path. Show Picture consumes separately hashed `runtimeMedia` images through the native HUD while preserving immutable Classic payloads; sound actions use the same boundary for WAV, Ogg Vorbis, and MP3 files. A fresh City of Bywater install displays producer-decoded PICT 32128 behind all four AP 76 messages, resolves the action's three stock sounds through Remake's native library, and hides the splash only when the authored Redraw Screen action runs. Other missing images produce a diagnostic and preserve progression. | Audit remaining scenario picture and sound references, then add focused pacing coverage where presentation order affects play. |
| Party conditions, random branches, allies, and registration | Partial | Source-backed, fixture-proven, native smoke | All ten party-condition slots have explicit signed ownership in the [condition compatibility matrix](CONDITION_COMPATIBILITY.md). Opcode `40` reads the exact state, including negative permanent values and the source-unused tenth slot; native global effects remain a display and mechanics projection for existing consumers. Positive values decay once per combat round or crossed field hour, negative values survive both reduction paths and save/load, and permanent Dragon Hide continues to reduce physical weapon damage. Discover Secret, Wizard Eye, Sentry, Thought Lace, Waterworld, and Free Fall use their existing native exploration, encounter, resistance, breathing, and fall-protection seams. A Classic-only Search toggle appears in the normal HUD, persists `-1`/`0`, switches the effect indicator, guarantees the native secret check, and pays the source four-tick cost on every map search pass. Readiness blocks referenced out-of-range slots before launch. Opcode `85` implements valid random ranges, but its only City of Bywater record is malformed and the readiness report identifies its signed missing Data EDCD row. Opcode `87` compares the live ally list by monster name byte; opcode `89` requires an exact compiled-to-native Data MD record identity. Producer-generated campaign bestiary entries now take priority over legacy numeric mappings and retain both identities through ordinary native creature initialization. The unmodified authoritative producer fixture now authors Add Ally with a carried and equipped scenario weapon; a Main-scene package smoke installs it, runs the real adapter, and round-trips the ally's display name, health, money, combat preference, Classic identity, and weapon through normal serialization. A broader derived smoke covers weighted spell slots and additional native item behavior. Older saves can recover a missing Bestiary key from Classic identity metadata. City of Bywater's Vodalian still resolves through the shared `Vodalian 71` entry. Unsupported fields on a referenced generated ally are a launch blocker under the same monster-readiness boundary used by battles and opcode `124`. Opcode `98` is a source-backed no-op. | Random branches and both ally outcomes have native playtests; party conditions are covered. |
| Classic spell parity | Partial | Fixture-proven | Opcodes `17` and `18` resolve each target independently, first apply the compatibility-owned all-or-nothing magic-resistance roll when the mapped or compiled spell permits it, then separately scale the authored damage-save adjustment by spell power, halve damaging spells on a successful save, negate saved condition effects, and honor force-affect. Exact-ID resources distinguish same-name variants. Existing native spells are reused only when their mechanics match; compatibility variants or data-driven resources cover representable differences. Native field-spell resources must declare their Classic save type and behavior, and readiness blocks mapped spells when required metadata is absent. All 20 queued-area damage IDs use exact `Data AD` masks, immediate-plus-persistent resolution, large-creature and stationary collision, caster-phase duration expiry, source queue artwork, and Classic's 60-field limit. The three Silence identities now use that same field lifecycle while preserving their per-target resistance and special-save differences and blocking ordinary player and monster spellcasting. Free Fall and Hover share Classic party condition `6`, preserve their distinct source duration rolls, replace only with a longer result, survive save/load, and decay once per combat round or crossed game-hour boundary while continuing to drive Remake's Feather Fall HUD and encounter-condition checks. All three Discover Secret identities use the same exact condition clock for condition `3`; while active, the existing Awareness effect now guarantees Remake's native secret-detection roll as it does in Classic. Wizard Eye uses condition `4` to let Remake's native exploration rays pass through sight-blocking tiles for the source duration. Thought Lace uses condition `8` to add Classic's 50-point bonus to player-character charm saves without affecting monsters. The two Sentry identities preserve their different source costs while sharing condition `7`, which suppresses only wandering battles and leaves scripted random encounters available. Sparkling Armor and all three Vorpal Plate identities share Classic character condition `7`: each remaining condition point subtracts two percentage points from melee hit chance, decays once per round or crossed game hour, persists through saves, respects the source condition caps, and does not alter ranged attacks. All seven special-`57` healing identities now preserve their source dice, costs, presentation, resistance bypass, and unconscious-versus-dead boundary through Remake's native health and life-status behavior. The six special-`56` Phase identities now share source-backed combat relocation, preserve their distinct action-ending rules, and retain Classic's lethal solid or occupied destination result. All eleven special-`58` summon identities select summonable monsters from the active campaign bestiary by Classic hit-dice tier, use the source retry fallback, inherit caster ownership and faction, preserve power-scaled exact-tile placement, enter native initiative, remain battle-only, and honor the 100-monster slot limit. Creature Summon 5 deliberately repairs its isolated zero-tier source defect to the described tier five; a campaign without summonable records reports the missing dependency instead of borrowing a shared monster. Weakness and Improved Power Drain use native ray or power-scaled target selection and the shared clamped spell-point mutation path; Weakness intentionally recovers its described `30-50` drain from the source record's misplaced duration fields instead of preserving the original no-op. Both Power Surge identities restore their source-scaled spell points without resistance or a save, clamp player characters to maximum, and preserve Classic's uncapped monster gain. Custom scenario rules remain a separate compatibility tier. Learned entries now retain exact Classic IDs and native resource keys across campaign entry and save/load. Legacy name-only entries use source-backed class/school evidence when sufficient; unresolved ambiguity preserves the saved implementation and emits a diagnostic. | Every player-facing core `Data S` identity has executable Remake behavior regardless of current scenario usage, including targeting, resistance, saves, force-affect, conditions, availability, and special effects. |
| Complex-encounter special responses | Partial | Fixture-proven | Action, spoken, spell, ordinary item, lock, trap, trap-spell, scroll-as-spell item, and door-activation responses work. Special items consume native charges, disposable scrolls leave inventory when empty, door items enter their compiled Data ED3 action point, and those resulting mutations survive the normal Classic save envelope. Readiness blocks missing trap/item spell resources and missing door action points. | Every response mode in the compiled complex-encounter schema selects, consumes, and persists its authored result through a normal Remake save/load round trip. |

## Recommended fidelity work

These items can remain behind a compatibility fallback while required work is
in progress. A scenario-specific audit may promote one to Required when its
presentation contains information needed for progression.

| Capability | Remake status | Evidence | Current fallback | Completion criterion |
| --- | --- | --- | --- | --- |
| Chance-based rogue lock interaction | Supported | Native playtest | By maintainer decision, Remake keeps a native chance roll instead of reproducing Classic's timed tumbler minigame. The HUD shows the selected character and computed chance, the resolver applies the Remake stat plus the Classic modifier and 90-percent cap, and success/failure results and consumed actions persist through snapshots. Native success and failure smokes cover keyboard focus/navigation and normal button activation. | Revisit only if the product direction changes; a timed tumbler minigame is intentionally out of scope. |
| Standalone Classic player maps | Partial | Native playtest | Immediate display requests use decoded runtime art, the compiled map name, and its note without requiring a native minimap. Maps/Notes browses acquired Classic records in stable ID order and keeps note-only records available when decoded art is missing. Terrain-composed records render a standalone 320 x 320 view from materialized native tiles with authored marker overlays and the current party marker. Producer-decoded scrolling TEXT records render as plain text while retaining immutable TEXT and paired styl identities; exact Classic style runs are not yet reproduced. A fresh City install opens both the initially owned map and the blacksmith's newly acquired map through the normal Maps/Notes control. Native minimaps remain the fallback for campaigns without acquired Classic records. | Broaden scenario coverage beyond City of Bywater and reproduce Classic text styling where it materially affects presentation. |
| Classic picture, sound, and UI pacing | Implemented | Source-backed, fixture-proven, native smoke | The City acceptance keeps decoded PICT 32128 visible through all four authored messages and three interleaved stock sounds, then hides it only at Redraw Screen. Opcode `124` now hides each new combatant until its own authored sound and neutral native conjuration reveal run in Classic order; the effect restores the original modulation and does not tint source art. Simple-encounter and rogue menus are centered and clamped inside the native map area, with keyboard focus and `1100x619` smoke coverage. Supported built-in warnings use their exact Classic text and warning sound, pause the existing continuation, and accept mouse or keyboard acknowledgement. Missing media remains a fidelity fallback unless an executable action carries `mediaRequiredForProgression`, which promotes the diagnostic to a source-located launch blocker. | Covered for the currently supported Classic presentation surface; new media modes require their own source audit and visual fixture. |
| Compatibility diagnostics | Partial | Fixture-proven | The headless readiness report separates progression blockers from fidelity fallbacks, includes source record context, and supports versioned JSON output. Campaign selection now presents the readiness state, summary, and first progression blocker, but the UI does not yet expose the complete diagnostic report. | Import or launch presents the readiness summary and complete diagnostic details without requiring the standalone command. |

## City of Bywater opcode coverage

All 2,734 active action slots in the checked City of Bywater bundle now have
defined interpreter behavior. This does not close the integration and fidelity
gaps above, or prove paths outside the trigger-slot coverage report.

Update this file when a boundary is discovered, reclassified, or completed.
Keep shipped-scenario counts in the runtime README so this register remains
useful across more than one converted campaign.
